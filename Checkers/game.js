// Generated by Creer, git hash 3387ee0c43f2ea6a7c76949338d58308dace22b8
// This is a simple class to represent the Game object in the game. You can extend it by adding utility functions here in this file.

var Class = require("../utilities/class");
var BaseGame = require("../baseGame");

// game object classes
var GameObject = require("./gameObject");
var Player = require("./player");
var Checker = require("./checker");

/// @class Game: The simple version of American Checkers. An 8x8 board with 12 checkers on each side that much move diagonally to the opposing side. Very simple.
var Game = Class(BaseGame, {
	/// initializes a Game with basic logic as provided by the Creer code generator
	// @param <object> data: initialization data
	init: function(data) {
		BaseGame.init.call(this, data);

		this.maxTurns = parseInt(data.maxTurns === undefined ? 100 : data.maxTurns);
		this.checkerMoved = (data.checkerMoved === undefined ? null : data.checkerMoved);
		this.checkers = (data.checkers === undefined ? [] : data.checkers);
		this.currentPlayers = (data.currentPlayers === undefined ? [] : data.currentPlayers);
		this.boardWidth = parseInt(data.boardWidth === undefined ? 8 : data.boardWidth);
		this.currentPlayer = (data.currentPlayer === undefined ? null : data.currentPlayer);
		this.gameObjects = (data.gameObjects === undefined ? {} : data.gameObjects);
		this.boardHeight = parseInt(data.boardHeight === undefined ? 8 : data.boardHeight);
		this.checkerMovedJumped = (data.checkerMovedJumped === undefined ? false : data.checkerMovedJumped);
		this.players = (data.players === undefined ? [] : data.players);
		this.currentTurn = parseInt(data.currentTurn === undefined ? 0 : data.currentTurn);

		this.name = "Checkers"

		this._gameObjectClasses = {
			"GameObject": GameObject,
			"Player": Player,
			"Checker": Checker,
		};
	},

});

module.exports = Game;
